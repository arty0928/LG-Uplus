가중치가 없을 때 -> BFS 가 더 빠름 O(N)


가중치가 있을 때 -> 다 방문해봐야 함
: 다익스트라 O(N^2)

dfs로 가려면 다 가야해서 3^((N-2)*(N-2))

모든 경우의 수를 구하라 -> DFS가 더 빠름 ( 재귀로 인자만 넘겨주면 되니까 )
BFS는 큐에 좌표를 넣고 빼야 하는 비용이 더 든다.

------------------
BFS
 - 가중치가 없는 map에서 s -> e 최소 비용 (최소 거리)를 구할 때  O(N)
 - 특정 위치에서 점진적으로 퍼져나가는 

 
DFS
 - s -> e 가는 모든 방법의 수를 구할 때 (BFS로 할 경우 객체를 만들어서 큐에 넣고 빼는 비용이 큼)