완전 탐색
	- Search
		Sequencial Search
		이진 탐색 (정렬된 데이터만)
		k 개 정해짐 -> sliding window
		k 개 정해지지 않음, 상황에 따라 -> 투 포인터
	
	
	- 경우의 수
		순열, 조합, 부분집합
	
	
	- 그래프, 트리
		dfs, bfs
		


순열, 조합, 부분집합, DFS 는 재귀로 가능

순열, 조합, 부분 집합, 경우의 수 => DP